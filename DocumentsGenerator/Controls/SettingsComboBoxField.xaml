<UserControl x:Class="DocumentsGenerator.Controls.SettingsComboBoxField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentsGenerator.Controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d">

    <Grid Background="Transparent"
            Height="30"
            MaxHeight="30"
            Margin="0 0 0 10"
            Width="650">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <Border Background="{Binding BackgroundColor, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Label Content="{Binding DisplayKey, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Foreground="White"
                    FontWeight="Medium"
                    FontSize="12"
                    VerticalAlignment="Center"/>
        </Border>

        <ComboBox x:Name="KeyFilterSelect"
                  Grid.Column="1"
                  VerticalContentAlignment="Center"
                  ItemsSource="{Binding FileKeyFilters, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding Value, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                  Style="{StaticResource CustomComboBox}"/>

    </Grid>
</UserControl>
