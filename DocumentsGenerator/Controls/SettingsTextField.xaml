<UserControl x:Class="DocumentsGenerator.Controls.SettingsTextField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentsGenerator.Controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d">

    <Grid Background="Transparent"
          Height="30"
          MaxHeight="30"
          Margin="0 0 0 10"
          Width="700">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <Border Background="{Binding BackgroundColor, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Label Content="{Binding DisplayKey, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="White"
                   FontWeight="Medium"
                   FontSize="12"
                   VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Column="1">
            <TextBox Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Background="#4B4789"
                     Foreground="White"
                     VerticalContentAlignment="Center"
                     FontSize="12"
                     Grid.ColumnSpan="2"
                     TextWrapping="WrapWithOverflow"
                     Padding="4 0 34 0"/>
        </Border>
        <local:IconButton IconImage="/Images/clear-icon.png"
                          Height="30"
                          Width="30"
                          Grid.Column="2"
                          IconHeight="25"
                          IconWidth="25"
                          Background="{Binding BackgroundColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Style="{StaticResource DefaultIconButtonTheme}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:CallMethodAction MethodName="Execute" />
                    <i:InvokeCommandAction
                        Command="{Binding ClearButtonCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"
                />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </local:IconButton>
        
        <local:IconButton x:Name="LoadValueBtn"
                          IconImage="/Images/folder-icon.png"
                          Visibility="{Binding LoadValueBtnVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Height="30"
                          Width="30"
                          Grid.Column="3"
                          IconHeight="20"
                          IconWidth="20"
                          Background="{Binding BackgroundColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Style="{StaticResource DefaultIconButtonTheme}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:CallMethodAction MethodName="Execute" />
                    <i:InvokeCommandAction
                        Command="{Binding SelectButtonCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </local:IconButton>

    </Grid>

</UserControl>
