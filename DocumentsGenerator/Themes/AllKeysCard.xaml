<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DocumentsGenerator.Controls"
                    xmlns:local="clr-namespace:DocumentsGenerator"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors">

    <Style TargetType="ListViewItem" x:Key="AllKeysCard">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Grid Background="Transparent"
                                Height="30"
                                MaxHeight="30"
                                Margin="0 0 0 10"
                                Width="600">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

       
                            <TextBox x:Name="TextBox"
                                    Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Background="#4B4789"
                                    Foreground="White"
                                    VerticalContentAlignment="Center"
                                    FontSize="12"
                                    TextWrapping="WrapWithOverflow"
                                    Padding="4 0 34 0"
                                    Visibility="{Binding TextBoxVisibility}"/>


                            <controls:IconButton IconImage="/Images/clear-icon.png"
                                                Height="30"
                                                Width="30"
                                                Grid.Column="1"
                                                IconHeight="25"
                                                IconWidth="25"
                                                Background="#7C7C7C"
                                                Style="{StaticResource DefaultIconButtonTheme}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:CallMethodAction MethodName="Execute" />
                                        <i:InvokeCommandAction 
                                            Command="{Binding DataContext.ClearValueCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </controls:IconButton>

                            <controls:IconButton IconImage="/Images/delete-icon.png"
                                                Height="30"
                                                Width="30"
                                                Grid.Column="2"
                                                IconHeight="25"
                                                IconWidth="25"
                                                Background="#7C7C7C"
                                                Style="{StaticResource DefaultIconButtonTheme}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:CallMethodAction MethodName="Execute" />
                                        <i:InvokeCommandAction 
                                            Command="{Binding DataContext.DeleteValueCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </controls:IconButton>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
</ResourceDictionary>
