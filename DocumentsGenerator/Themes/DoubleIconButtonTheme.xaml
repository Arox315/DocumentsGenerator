<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DocumentsGenerator.Controls">
    <Style TargetType="{x:Type local:DoubleIconButton}"
        x:Key="DoubleIconButtonTheme">

        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type local:DoubleIconButton}">
                        <Grid VerticalAlignment="Stretch"
                                    HorizontalAlignment="Stretch"
                                    Background="Transparent">
                            
                            <Border x:Name="BG"
                                    Background="White"
                                    Opacity="0"
                                    Height="{TemplateBinding Height}"
                                    Width="{TemplateBinding Width}"
                                 />

                            <StackPanel Orientation="Horizontal">
                                <Image Source="{TemplateBinding IconImage1}"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       Width="20"
                                       Height="20"
                                       Stretch="UniformToFill"/>

                                <Image Source="{TemplateBinding IconImage2}"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       Width="30"
                                       Height="30"
                                       Stretch="UniformToFill"
                                       HorizontalAlignment="Left"/>
                            </StackPanel>
                            
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BG"
                                     Property="Opacity"
                                     Value="0.2"
                                     />
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style.Setters>
    </Style>
</ResourceDictionary>