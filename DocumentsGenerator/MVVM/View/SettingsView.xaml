<UserControl x:Class="DocumentsGenerator.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentsGenerator.Controls"
             xmlns:view="clr-namespace:DocumentsGenerator.MVVM.View"
             xmlns:viewModel="clr-namespace:DocumentsGenerator.MVVM.ViewModel"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <viewModel:SettingsViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="75"/>
        </Grid.RowDefinitions>

        <ListView Background="Transparent"
                  Margin="10"
                  BorderThickness="0"
                  >

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <!-- template settings -->
            <ListViewItem HorizontalContentAlignment="Center">
                <TextBlock Text="Generacja szablonów"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Medium"
                           TextAlignment="Center"
                           Margin="5"/>
            </ListViewItem>
            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding TemplateDefaultFileKeyName.DisplayName}" Value="{Binding TemplateDefaultFileKeyName.Value}" BackgroundColor="{Binding MainColor}" LoadValueBtnVisibility="Collapsed"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsComboBoxField DisplayKey="{Binding TemplateDefaultFileKeyFilter.DisplayName}" Value="{Binding TemplateDefaultFileKeyFilter.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding TemplateFileSelectorInitialDirectory.DisplayName}" Value="{Binding TemplateFileSelectorInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>
            
            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding TemplateFolderSelectorInitialDirectory.DisplayName}" Value="{Binding TemplateFolderSelectorInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>
            
            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding TemplateSaveFolderSelectInitialDirectory.DisplayName}" Value="{Binding TemplateSaveFolderSelectInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>
            
            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding TemplateSaveFolderDirectory.DisplayName}" Value="{Binding TemplateSaveFolderDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <!-- data sheet settings -->
            <ListViewItem HorizontalContentAlignment="Center">
                <TextBlock Text="Generacja arkuszy"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Medium"
                           TextAlignment="Center"
                           Margin="5"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DataSheetDefaultFileKeyName.DisplayName}" Value="{Binding DataSheetDefaultFileKeyName.Value}" BackgroundColor="{Binding MainColor}" LoadValueBtnVisibility="Collapsed"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsComboBoxField DisplayKey="{Binding DataSheetDefaultFileKeyFilter.DisplayName}" Value="{Binding DataSheetDefaultFileKeyFilter.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DataSheetFileSelectorInitialDirectory.DisplayName}" Value="{Binding DataSheetFileSelectorInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DataSheetFolderSelectorInitialDirectory.DisplayName}" Value="{Binding DataSheetFolderSelectorInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DataSheetSaveFolderSelectInitialDirectory.DisplayName}" Value="{Binding DataSheetSaveFolderSelectInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DataSheetSaveFolderDirectory.DisplayName}" Value="{Binding DataSheetSaveFolderDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>
            
            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DataSheetEditInitialDirectory.DisplayName}" Value="{Binding DataSheetEditInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <!-- ducument settings -->
            <ListViewItem HorizontalContentAlignment="Center">
                <TextBlock Text="Generacja dokumentów"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Medium"
                           TextAlignment="Center"
                           Margin="5"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DocumentDefaultFileKeyName.DisplayName}" Value="{Binding DocumentDefaultFileKeyName.Value}" BackgroundColor="{Binding MainColor}" LoadValueBtnVisibility="Collapsed"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsComboBoxField DisplayKey="{Binding DocumentDefaultFileKeyFilter.DisplayName}" Value="{Binding DocumentDefaultFileKeyFilter.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DocumentFileSelectorInitialDirectory.DisplayName}" Value="{Binding DocumentFileSelectorInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DocumentFolderSelectorInitialDirectory.DisplayName}" Value="{Binding DocumentFolderSelectorInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DocumentSaveFolderSelectInitialDirectory.DisplayName}" Value="{Binding DocumentSaveFolderSelectInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DocumentSaveFolderDirectory.DisplayName}" Value="{Binding DocumentSaveFolderDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>
            
            <ListViewItem HorizontalContentAlignment="Center">
                <local:SettingsTextField DisplayKey="{Binding DocumentLoadDataSheetInitialDirectory.DisplayName}" Value="{Binding DocumentLoadDataSheetInitialDirectory.Value}" BackgroundColor="{Binding MainColor}"/>
            </ListViewItem>

        </ListView>
        
        <!-- save btn -->
        <Button 
            Width="150" 
            Height="40" 
            Grid.Row="1"
            Content="Zapisz"
            HorizontalAlignment="Right"
            Margin="0 0 20 0"
            Foreground="White"
            FontSize="16"
            FontWeight="Bold"
            Background="{Binding MainColor}"
            Command="{Binding SaveSettingsCommand}"
            Style="{StaticResource MainButtonTheme}"/>
    </Grid>
</UserControl>
